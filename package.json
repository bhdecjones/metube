{
  "name": "metube-monorepo",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.12.3",
  "scripts": {
    "lint": "pnpm -r lint",
    "dev": "pnpm --filter @metube/web dev",
    "build": "pnpm -r build"
  },
  "engines": {
    "node": "20.16.0"
  }
}
