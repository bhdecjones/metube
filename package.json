{
  "name": "metube-monorepo",
  "private": true,
  "version": "0.1.0",
  "packageManager": "npm@10.9.0",
  "scripts": {
    "lint": "npm run lint --workspaces --if-present",
    "dev": "npm run dev --workspace @metube/web",
    "build": "npm run build --workspaces --if-present"
  },
  "engines": {
    "node": "20.x"
  },
  "workspaces": [
    "apps/*",
    "packages/*",
    "backend"
  ]
}
