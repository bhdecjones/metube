{
  "name": "mytube-monorepo",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.12.3",
  "scripts": {
    "lint": "pnpm -r lint",
    "dev": "pnpm --filter @mytube/web dev",
    "build": "pnpm -r build"
  },
  "engines": {
    "node": "22.x"
  }
}
