{
  "name": "mytube-monorepo",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@8",
  "scripts": {
    "lint": "pnpm -r lint",
    "dev": "pnpm --filter @mytube/web dev",
    "build": "pnpm -r build"
  }
}
